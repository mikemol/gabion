[pytest]
testpaths = tests
addopts = -n auto --dist=loadfile --import-mode=importlib
markers =
    live_repo_signal: repo-state sentinel tests whose purpose is to observe live repository state rather than synthetic semantic contracts
