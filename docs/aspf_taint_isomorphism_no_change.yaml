in_steps:
  - in-46
  - in-47
  - in-48
  - in-49
  - in-50
  - in-51
  - in-52
  - in-53
  - in-58
  - in-59
  - in-60
  - in-61
  - in-62
  - in-63
  - in-64
  - in-65
  - in-66
  - in-67
  - in-68
  - in-69
  - in-70
  - in-71
  - in-72
  - in-73
  - in-74
  - in-75
  - in-76
  - in-77
  - in-78
  - in-79
  - in-80
  - in-81
  - in-82
  - in-83
  - in-84
  - in-85
  - in-86
  - in-87
  - in-88
  - in-89
  - in-90
  - in-91
  - in-92
  - in-93
  - in-94
  - in-95
  - in-96
  - in-97
  - in-98
  - in-99
  - in-100
  - in-101
  - in-102
  - in-103
  - in-104
  - in-105
  - in-106
  - in-107
  - in-108
  - in-109
  - in-110
  - in-111
  - in-112
delta_note_8: "Retiring the temporary `semantic_carrier_adapter` grading on `close_canonical_semantic_row(...)` while keeping `reflect_projection_fiber_witness(...)` as the lawful reflection boundary preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing `semantic_fragment.py` to the two remaining PSF-tracked seams: reflection plus shared canonical-value materialization."
delta_note_9: "Adding shared kernel-object image carriers plus explicit `AugmentedRule` bindings across the ASPF, semantic-fragment, lowering, and compile layers preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while making the TTL-kernel runtime-image residue observable without changing existing witness semantics or crosswalk rows."
delta_note_10: "Publishing a shared public lenient YAML frontmatter helper and routing `impact_index.py`, `normative_symdiff.py`, and script-side document scanners through that boundary-normalization utility preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing parser duplication and governance-private frontmatter imports."
delta_note_11: "Routing ASPF execution-fibration payload mapping, test-obsolescence delta parsing, normative-symdiff list/mapping/string coercion, server-core progress parsing, and CLI resume/progress coercion through the shared runtime coercion contract preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while collapsing duplicate runtime-shape ingress logic onto one boundary-normalization substrate."
delta_note_12: "Reifying `dataflow_indexed_file_scan.py` as a boundary-only alias facade backed by an explicit alias-inventory materialization helper preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the legacy monolith import hub into a compatibility-only adapter surface with observable retirement telemetry."
delta_note_13: "Splitting the indexed dataflow alias inventory into explicit owner-domain adapter modules plus a shared alias-contract substrate preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while deflating the remaining compatibility-surface fan-in without changing the legacy monolith import surface or emitted crosswalk obligations."
delta_note_14: "Conditioning final post-phase completion emission on a real post-work boundary and rendering annotation-drift summary mappings to JSON at the report producer preserve the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while correcting progress/report boundary discipline without changing ASPF identities or crosswalk obligations."
delta_note_15: "Adding a first-class landed marker lifecycle plus closure-consistency validation for planning-substrate workstream metadata preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings because the change is governance/control-loop metadata hardening over invariant markers rather than a modification of ASPF witness semantics or taint row identities."
delta_note_16: "Reifying orchestration/dataflow option bundles and serializing semantic-coverage report payloads at the report-output boundary preserve the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while tightening ingress/output contracts without changing emitted ASPF identities or taint crosswalk semantics."
delta_note_17: "Repairing helper-level match exhaustiveness in forest-signature, type-fingerprint, config-field, report-section, structure-snapshot, handledness, and test-evidence normalization surfaces preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing contradictory reachable-then-never helper fall-throughs; the change tightens helper contracts but does not alter emitted ASPF identities or taint crosswalk semantics."
delta_note_18: "The SAC fixed-point sweep that rewrites dataflow/reporting collector helpers, invariant-graph tuple staging, and policy-rule AST classifier fallthroughs into filter-first or typed-dispatch forms preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings because it tightens internal helper structure only and does not alter emitted ASPF identities or taint crosswalk semantics."
delta_note_6: "Expanding `impact_index.py` symbol scanning to include `scripts/` and rooting runtime perf heat on the shared doc-target selector preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while converging doc-to-symbol selection and profiler-query rooting onto one selector substrate."
delta_note_7: "Collapsing projection execution-op normalization back into the graded `execution_ops_from_spec(...)` entrypoint while tightening repo/workstream frontier invariants preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings; the change narrows internal planner helper seams, resolves root-provenance/lane-target planning metadata upstream, and does not alter emitted ASPF identities or crosswalk obligations."
delta_note_5: "Prime-backed queue identities now intern decomposition views as typed carriers, recursive policy artifact streams render typed identities only at JSON/Markdown boundaries, and call-cluster summary/consolidation carriers now keep canonical prime-backed cluster identities internally while preserving the same emitted wire identities; these changes preserve the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while tightening boundary rendering and internal identity discipline."
delta_note_4: "Shared invariant-marker scanning, invariant-graph projection, and PSF-007 queue projection now consume governance-only marker metadata plus global site/structural identity carriers, while `dataflow_projection_materialization.py` routes its remaining fixed-spec materialization through typed execution-op planning/execution rather than the removed `apply_spec(...)`; these changes preserve the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings."
delta_note_3: "Extracting strict YAML frontmatter parsing into a shared helper and reusing it from `impact_index.py` plus the policy DSL compiler preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while moving markdown-frontmatter parsing onto a shared boundary-normalization utility."
delta_note_2: "Collapsing the quotient-face field-plan forwarding wrapper into the already-graded `compile_projection_fiber_quotient_face_*` entrypoints preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing a second quotient-face assembly seam."
delta_note: "Collapsing `_witness_trace(...)` into the already-graded `compile_projection_fiber_*` entrypoints preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing a helper-only witness-forwarding seam."
justification: marker protocol generalization now classifies todo/deprecated marker kinds in runtime never/todo/deprecated markers and static alias-map extraction while preserving crosswalk row coverage; current taint diagnostic and marker-ingress refinements preserve existing ASPF crosswalk obligations and identifier anchors; decision-protocol module markers introduced for policy compliance do not alter marker/ASPF identifier mappings; boundary-normalization markers on taint/ci-watch payload extraction helpers are policy-annotation only and do not alter ASPF identifier mappings or taint crosswalk semantics; marker reason coercion to string at payload normalization is runtime-hardening for invariant throws and does not change ASPF identifier anchors or taint crosswalk semantics; added aspf_rule_engine adapter projects witness booleans into DSL input only and does not modify existing ASPF identifier anchors or taint crosswalk mappings; derived identity adapter lifecycle metadata added to fingerprint identity layers/aliases is boundary-governance annotation only and does not modify ASPF taint identifier mappings or crosswalk semantics; derived alias payload construction now reuses identity-layer projection contracts via a helper and does not alter ASPF taint mappings or crosswalk semantics; derived-alias payload construction now uses strict projection-key access via helper construction and preserves the same ASPF taint/crosswalk mappings; lifecycle metadata now reified via a typed dataclass contract before projection to payload dicts, preserving identical ASPF taint/crosswalk mappings; derived-alias payload enforcement now validates required projection fields and lifecycle mapping shape before payload projection, preserving identical ASPF taint/crosswalk mappings; identity-layer projection validation now enforces canonical/projection/adapter-name invariants prior to alias projection and does not alter ASPF taint/crosswalk mappings; FingerprintIdentityLayers now validates on as_dict/derived_aliases emission to enforce projection invariants at boundary projection time without changing ASPF taint/crosswalk semantics; derived identity projections are now reified as typed dataclasses before emission/alias projection while preserving identical ASPF taint and crosswalk mappings; projection contract constants now reify expected projection/adapter bindings for identity-layer validation without changing ASPF taint/crosswalk semantics; derived alias payloads now retain typed lifecycle carriers until final serialization, preserving unchanged ASPF taint/crosswalk mappings; projection-contract validation now uses typed contract bindings table for deterministic per-projection checks without altering ASPF taint/crosswalk mappings; projection bindings are now reified as typed entries and validated through explicit binding iteration without changing ASPF taint/crosswalk semantics; projection slot resolution now uses typed enum slots and exhaustive matching, preserving unchanged ASPF taint/crosswalk semantics; canonical identity layer ingress now normalizes into a typed CanonicalAspfPathPayload contract (representative/basis_path guards) before derived projection emission, preserving existing ASPF taint/crosswalk mappings while strengthening boundary invariants; canonical identity payload validation now enforces source/target pair coherence and suite_site_endpoints source+target mapping presence before identity-layer emission, preserving ASPF taint/crosswalk mappings while tightening boundary contract guarantees; canonical suite-site endpoint ingress is now normalized to require non-empty kind and non-empty key segments for both source/target endpoints before identity-layer serialization, preserving ASPF taint/crosswalk mappings while tightening endpoint-shape invariants; canonical suite-site endpoint validation now enforces source/target endpoint-key first-segment coherence with canonical source/target labels, preserving ASPF taint/crosswalk mappings while tightening endpoint identity alignment invariants; canonical suite-site endpoint normalization now requires endpoint kind to be exactly SuiteSite for both source/target endpoints, preserving ASPF taint/crosswalk mappings while tightening endpoint-kind invariants; canonical suite-site endpoint validation now requires source endpoint key to terminate with source role and target endpoint key to terminate with target role, preserving ASPF taint/crosswalk mappings while tightening endpoint-role coherence invariants; timeout flow batching retunes, non-throwing todo debt markers, PR-412 opportunity annotations, and match-fallthrough strictifications in reporting/index/helpers adjust deadline responsiveness and boundary/control contracts only, and do not alter ASPF taint identifiers, witness labels, or crosswalk row mappings; projection semantic-fragment bootstrapping now adds a canonical witnessed semantic carrier and projection-fiber frontier adapter over the existing ASPF/fibration identities and witness substrate, preserving ASPF taint identifier anchors and crosswalk row mappings while exposing a new semantic-row surface on top of the existing carriers; semantic-carrier adapter boundary-kind recognition for grade monotonicity now classifies carrier-materialization adapters as explicit boundary surfaces only and does not alter ASPF taint identifier anchors, witness labels, or crosswalk row mappings; projection-fiber reflect compilation now lowers canonical semantic rows into typed SHACL/SPARQL plan carriers derived from the existing ASPF/fibration-backed semantic row without altering ASPF taint identifier anchors, witness labels, or crosswalk row mappings; authoring-layer quotient-face compilation now lowers declared projection_fiber.frontier semantic ops into typed SHACL/SPARQL plans over the same canonical projection-fiber semantic rows, preserving existing ASPF taint identifier anchors, witness labels, and crosswalk row mappings; registered projection-fiber frontier authoring specs and lattice-convergence semantic-plan bundles now reuse that same carrier/compiler path inside policy artifacts, preserving existing ASPF taint identifier anchors, witness labels, and crosswalk row mappings; policy-suite semantic summaries, frontier-preview joins, and hotspot-queue propagation now repackage existing projection-fiber semantic-plan metadata from policy artifacts without altering ASPF taint identifier anchors, witness labels, or crosswalk row mappings; projection-fiber reflect is now promoted through the shared typed lowering/compiler path via a registered reflection spec, replacing the former direct substrate compile branch without altering ASPF taint identifier anchors, witness labels, or crosswalk row mappings; projection_exec compatibility now erases semantic-only ops and semantic metadata at execution ingress without altering ASPF taint identifier anchors, witness labels, or crosswalk row mappings; extracting that ingress normalization into the dedicated projection_exec_ingress adapter preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the legacy executor surface; typing execution-step params before they reach projection_exec preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing late dynamic param parsing from the executor; moving `ProjectionSpec` application fully into `projection_exec_ingress.py` and keeping `projection_exec.py` typed-step-only preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the compatibility surface again; promoting `support_reflect(surface=projection_fiber)` through the shared typed lowering/compiler path reuses existing projection-fiber witness/support/boundary context already carried on canonical semantic rows and does not alter ASPF taint identifier anchors, witness labels, or crosswalk row mappings; promoting `synthesize_witness(surface=projection_fiber)` through the shared typed lowering path adds a semantic-core-only witness-synthesis bundle over existing canonical rows without altering ASPF taint identifier anchors, witness labels, or crosswalk row mappings; promoting `wedge(surface=projection_fiber)` through the shared typed lowering/compiler path reuses existing projection-fiber input/synthesized/boundary/transform context on canonical semantic rows and does not alter ASPF taint identifier anchors, witness labels, or crosswalk row mappings; promoting `reindex(surface=projection_fiber)` through the shared typed lowering/compiler path reuses existing canonical branch, data-anchor, and execution-frontier identities already carried on projection-fiber rows and does not alter ASPF taint identifier anchors, witness labels, or crosswalk row mappings; promoting `existential_image(surface=projection_fiber)` through the shared typed lowering/compiler path reuses existing canonical synthesized-witness, boundary, and obligation-state context already carried on projection-fiber rows and does not alter ASPF taint identifier anchors, witness labels, or crosswalk row mappings; promoting `negate(surface=projection_fiber)` through the shared typed lowering/compiler path reuses that same canonical synthesized-witness, boundary, and obligation-state context to realize the negated existential-image anti-join without altering ASPF taint identifier anchors, witness labels, or crosswalk row mappings; moving fixed-spec report-section and annotation-drift presentation consumers onto lazy cached typed execution-op plans preserves existing ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on those stable rendering paths; moving the fixed-spec `call_cluster_consolidation` presentation consumer onto lazy cached typed execution-op plans preserves those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on that stable consolidation/reporting path; moving the fixed-spec `call_clusters` summary consumer onto lazy cached typed execution-op plans preserves those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on that stable call-cluster summary path; moving the fixed-spec `test_obsolescence` summary consumer onto lazy cached typed execution-op plans preserves those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on that stable stale/active summary path; moving the fixed-spec `LINT_FINDINGS_SPEC` projection path onto lazy cached typed execution-op plans preserves those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on that stable lint-row projection path; moving the fixed-spec `AMBIGUITY_SUMMARY_SPEC` summary path onto lazy cached typed execution-op plans preserves those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on that stable ambiguity-summary projection path; moving the fixed-spec `docflow_canonicality_ambiguity` reporting path onto lazy cached typed execution-op plans preserves those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking the temporary ProjectionSpec adapter surface on that stable governance-reporting path; normalizing internal docflow invariants into typed predicate matchers and evaluating them directly in governance preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another internal ProjectionSpec compatibility seam; retiring the internal `call_clusters` custom summary-spec override seam and keeping that path on one fixed typed execution-op plan preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another internal ProjectionSpec compatibility seam; extracting pure `ProjectionSpec`->typed-execution-op planning into a non-boundary internal module and repointing fixed-spec in-repo consumers away from `projection_exec_ingress.py` preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking fake internal `semantic_carrier_adapter` boundaries; retiring the dedicated `projection_exec_ingress.py` module preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing one more faux internal ingress surface; retiring the planner-local production `apply_spec(...)` convenience wrapper and composing planner plus typed execution only in tests preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking one more faux internal ProjectionSpec adapter seam; retiring the semantic-fragment and semantic-lowering module-level `semantic_carrier_adapter` markers in favor of function-local carrier planner/materializer grade markers preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another blanket internal cutover seam; retiring the broad `projection_exec.py` module-level `semantic_carrier_adapter` marker in favor of function-local typed-execution grade markers preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another blanket internal cutover seam; collapsing planner helper-only dict-copy and traverse/sort normalization seams back into the top-level `projection_exec_plan.py` planner preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another temporary internal `semantic_carrier_adapter` justification; collapsing the semantic-fragment transform-trace payload helper back into the real reflection/closure carrier surfaces preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another temporary internal `semantic_carrier_adapter` justification; collapsing the semantic-fragment boundary-trace and obligation-state helpers back into `reflect_projection_fiber_witness(...)` preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking two more temporary internal `semantic_carrier_adapter` justifications; treating `_input_witness_payloads(...)` as an ordinary internal witness-shaping helper rather than a graded carrier seam likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure/canonicalization helpers; treating `_join_payload(...)` as an ordinary internal join/meet witness-shaping helper rather than a graded carrier seam likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure/canonicalization plus naturality/obligation materialization helpers; treating `_naturality_payload(...)` as an ordinary internal naturality-witness shaping helper rather than a graded carrier seam likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure/canonicalization plus obligation materialization helpers; treating `_obligation_payloads(...)` as an ordinary internal obligation-state shaping helper rather than a graded carrier seam likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure/canonicalization helpers; collapsing the private `_normalize_object(...)` helper seam back into the row-closure and stable-unique callers likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure, recursive canonicalization, and stable-key materialization surfaces; collapsing the private `_stable_unique_objects(...)` helper seam back into `close_canonical_semantic_row(...)` likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure, recursive canonicalization, and stable-key materialization surfaces; collapsing the private `_normalize_mapping(...)` and `_normalize_list(...)` helper seams back into `_normalize_value(...)` likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the semantic-fragment cutover surface to the still-real row-closure/canonicalization and stable-key materialization helpers; collapsing the executor-side normalized-op relay and runtime-params copy seams back into `apply_execution_ops(...)` preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another temporary internal `semantic_carrier_adapter` justification without hiding the still-real sort/canonical-group-reference helpers; keeping `call_clusters` on a strict internal DTO until final render/write emission and treating markdown render plus server-side JSON serialization as the only real whole-Gabion output carrier conversions preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing faux internal ambiguity-boundary claims from the call-cluster summary path; `call_clusters` JSON emission is now centralized at the carrier module and preserves cluster identity at the output edge, which still does not alter ASPF taint identifier anchors, witness labels, or crosswalk row mappings; `count_by` grouping now uses a reversible canonical reference carrier for nested execution values rather than a hash-style classifier, preserving the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while tightening identity/reference semantics on the executor path; normalizing `test_evidence.json` once into a strict `TestEvidenceDocument` carrier at file ingress preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while moving the JSON->DTO transformation onto a real whole-Gabion I/O boundary; consolidating that shared file ingress at the loader while leaving `call_cluster_consolidation` marked only for its output materialization edge preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing a duplicate faux input-boundary claim; `call_cluster_consolidation` now also keeps a strict internal DTO until markdown/JSON emission, which preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while removing another raw-mapping compatibility surface from the call-cluster data fiber; collapsing repeated call-cluster identity and spec-provenance structure onto shared internal dataclass carriers likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings; and removing internal per-entry/per-cluster count carriers so call-cluster counts are derived only at ordering/output consumption sites preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while keeping iterable carriers authoritative inside Gabion; retiring the per-op execution-op classification helper's own `semantic_carrier_adapter` marker in `projection_exec_plan.py` likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while narrowing the planner cutover surface to the true top-level `ProjectionSpec` planning entrypoint; removing the dedicated graded `LimitExecutionOp` helper seam while discharging impossible negative counts upstream in `execution_ops_from_spec(...)` likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while shrinking another temporary executor-local grading surface; collapsing the compiler-side `_input_witness_kinds(...)`, `_synthesized_witness_kinds(...)`, `_boundary_kinds(...)`, and `_transform_ops(...)` forwarding seams back into the already-graded `compile_projection_fiber_*` entrypoints preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while keeping temporary grading concentrated on `_distinct_mapping_values(...)`, support-context serialization, and the real compile surfaces; collapsing `_synthesized_witness_payloads(...)` back into `reflect_projection_fiber_witness(...)` likewise preserves the same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while keeping the real work-growth grading on the reflection carrier entrypoint rather than on a second witness-assembly wrapper; organizing PSF-007 continuation state through todo-decorated subqueue/touchpoint registry symbols plus AST-derived semantic-carrier touchsite identities preserves existing ASPF taint identifier anchors, witness labels, and crosswalk row mappings because it adds governance-only continuation metadata over already-marked projection functions without altering the witness carriers, lowering semantics, or crosswalk row identities themselves; and the shared kernel-object image substrate plus explicit `AugmentedRule` bindings across the ASPF, semantic-fragment, lowering, and compile layers preserve those same ASPF taint identifier anchors, witness labels, and crosswalk row mappings while making the TTL-kernel runtime-image residue observable without changing witness semantics or crosswalk rows.
